import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    alignItems:'center',
    justifyContent:'flex-end'
  },
  canvas:{
    width:257,
    height:249,
    position:'absolute',
    zIndex:1
  }
})